<div class="container mx-auto px-4 py-12 max-w-4xl">
  <h1 class="text-4xl font-heading font-bold mb-8">{{ page.title }}</h1>
  <div class="prose prose-lg text-muted-foreground leading-relaxed">
    {{ page.content }}
  </div>
</div>
